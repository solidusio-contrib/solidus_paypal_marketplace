<% admin_breadcrumb(link_to plural_resource_name(Spree::Seller), spree.admin_users_path) %>
<% admin_breadcrumb(@seller.name) %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @seller } %>

<%= form_for [:admin, @seller], method: :post, url: admin_sellers_path, html: { multipart: true } do |f| %>
  <fieldset data-hook="new_seller">
    <legend align="center"><%= t('spree.new_seller') %></legend>

    <%= render partial: 'form', locals: { f: f } %>

    <%= render partial: 'spree/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
