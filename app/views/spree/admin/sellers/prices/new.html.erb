<% admin_breadcrumb(link_to t('spree.offers'), collection_url) %>
<% admin_breadcrumb(t('spree.new_offer')) %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @seller } %>

<%= form_for @price, url: collection_url do |f| %>
  <fieldset data-hook="new_offer">
    <legend align="center"><%= t('spree.new_offer') %></legend>

    <%= render 'form', f: f %>

    <%= render partial: 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
